<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Thème de base stable pour application Compose -->
    <style name="Theme.Buche" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Configuration minimale pour éviter les conflits avec Compose -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>